// Webpack entry point. The actual component is exposed via Module
// Federation (see webpack.config.js) and loaded by the signalk-server admin
// UI via the `signalk-plugin-configurator` keyword on the plugin's
// package.json.
